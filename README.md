# Grant-MailboxDelegation
PowerShell script to bulk-grant Full Access and Send As permissions on Exchange Online mailboxes to a delegate user from a simple text file. Idempotent, supports -WhatIf and CSV reporting.
